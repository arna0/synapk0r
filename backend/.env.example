# Copy to .env and fill in. Never commit .env.
ANTHROPIC_API_KEY=
# Optional
# CLAUDE_MODEL=claude-opus-5
# PORT=8787
# ALLOWED_ORIGINS=*            # comma-separated list, e.g. https://arna0.github.io
# TRUST_PROXY=1               # behind Render/Nginx: real client IP for rate limiting
# RATE_LIMIT_PER_MIN=20
