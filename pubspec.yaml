name: jobsim_ai
description: "JobSim AI - 3D Mobile Profession Simulator & AI Analytics MVP"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  http: ^1.2.0
  google_fonts: ^6.1.0
  percent_indicator: ^4.2.3
  webview_flutter: ^4.7.0
  flutter_spinkit: ^5.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
